stage: warmup
benchmark: libero
cache_manifest: local_data/token_caches/libero_progress_warmup/manifest.json
output_dir: local_data/runs/progress_warmup/libero_warmup_w4
device: cuda
batch_size: 64
max_steps: 1000
samples_per_epoch: 8192
sampling_alpha: 0.5
lr: 1.0e-4
weight_decay: 1.0e-4
grad_clip_norm: 1.0
num_workers: 0
seed: 42
deterministic: false
hidden_dim: 896
state_dim: 8
action_dim: 7
replan_stride: 16
latent_dim: 128
action_summary_hidden_dim: 512
state_hidden_dim: 512
updater_hidden_dim: 1792
planner_ffn_dim: 3584
planner_layers: 2
num_heads: 8
dropout: 0.05
lambda_plan: 1.0
lambda_stage: 0.5
lambda_mem_pool: 0.1
lambda_order: 0.02
use_order_loss: false
min_order_gap: 2
cosine_weight: 0.1
val_fraction: 0.1
split_by_episode: true
eval_interval: 50
eval_batch_size:
max_val_batches:
log_interval: 10
ckpt_interval: 0
repo_root: .
